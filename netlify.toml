# Netlify deploy config (works as-is if you host on Netlify).
[build]
  command = "npm run build"
  publish = "_site"

[build.environment]
  NODE_VERSION = "20"

# Pretty 404
[[redirects]]
  from = "/*"
  to = "/404.html"
  status = 404
